import React from 'react';
import PropTypes from 'prop-types';
import './Post.scss';

const Post = () => (
  <div className="Post">
    Post Component
  </div>
);

Post.propTypes = {};

Post.defaultProps = {};

export default Post;
